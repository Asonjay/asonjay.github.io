'use client'

// Contact section is now integrated into the homepage glass panel layout.
// This component is kept for backwards compatibility but renders nothing
// since the contact CTA is now part of page.tsx directly.

export function Contact() {
  return null
}
